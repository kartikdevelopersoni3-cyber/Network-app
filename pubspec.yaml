# The pubspec.yaml file is used to define the Flutter app's dependencies and metadata.

name: network_app
description: A new Flutter project.

# Environment settings
environment:
  sdk: ">=2.12.0 <3.0.0"

# Dependencies
dependencies:
  flutter:
    sdk: flutter
    cupertino_icons: ^1.0.8
  device_info_plus: ^10.1.2
  battery_plus: ^6.0.2
  webview_flutter: ^4.8.0
  connectivity_plus: ^6.0.5

# Flutter commands
flutter:
  uses-material-design: true
  assets:
    - assets/images/
